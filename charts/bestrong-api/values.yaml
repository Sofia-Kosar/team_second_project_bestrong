replicaCount: 2 # Вимога: 2 інстанси

image:
  repository: acrbestrong01.azurecr.io/bestrong-api
  tag: "latest"

service:
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  annotations: {} 
  hosts:
    - host: "" 
      paths:
        - path: /
          pathType: Prefix

canary:
  enabled: true
  weight: 10

env:
  connectionString: ""